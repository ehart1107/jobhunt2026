// Edit this file to make this copy of Docket your own.
// Nothing else needs to change — app.js reads these values automatically.

const DOCKET_CONFIG = {
  // Shown as the big heading in the sidebar and as the browser tab title.
  siteTitle: "The Docket",

  // Shown underneath the site title in the sidebar.
  siteTagline: "One file per application.",

  // The main accent colour — used for the active stage tab, focus outlines,
  // and highlighted fields. Any hex value works.
  accentColor: "#B4863F"
};
